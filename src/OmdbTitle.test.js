import React from 'react';
import { render, screen } from '@testing-library/react';

import OmdbTitle from './OmdbTitle';

describe('OmdbTitle', () => {
  test.todo('renders OmdbTitle component');
  test.todo('correctly renders fallback icon when poster is missing');
  test.todo('correctly renders Ratings array');
});
